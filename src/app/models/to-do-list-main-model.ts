import {TodoListModel} from './to-do-list-model';
import {ProfileModel} from './profile-model';

export class TodoListMainModel {
  data?: TodoListModel;
  employee?: ProfileModel;
  isActiveTr?: string;
  actionType?: string;
}
